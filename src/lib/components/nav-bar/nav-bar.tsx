import { routes } from "$utils/constants";

const NavBar = () => {
  return (
    <div id="nav-container">
      <span id="title"></span>
      <span id="routes"></span>
    </div>
  );
};

export default NavBar;
